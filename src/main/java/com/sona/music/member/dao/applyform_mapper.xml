<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>
<mapper namespace="com.sona.music.member.dao.ApplyFormDAO">

<insert id="apply" parameterType="map">
    INSERT INTO APPLICATION_FORM (INST_CATEGORY_IDX, APPLYFORM_INST, APPLYFORM_LOCATION, APPLYFORM_DAYS,
    	 HAVE_INST, APPLYFORM_STYLE, USER_ID)
    VALUES (#{instCategory}, #{inst}, #{location}, #{class_days}, #{have}, #{class_style}, #{user_id})
</insert>



</mapper>